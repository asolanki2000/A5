<link rel="stylesheet" href="/css/site.css">

<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>500</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;display:grid;place-items:center;height:100dvh;margin:0;background:#0b1220;color:#fff}
      .card{max-width:720px;padding:24px;border-radius:16px;background:#101826;border:1px solid #1f2a44}
      .msg{white-space:pre-wrap;word-break:break-word;color:#d1d5db}
    </style>
  </head>
  <body>
    <div class="card">
      <h1>500 — Something went wrong</h1>
      <% if (typeof message !== 'undefined' && message) { %>
        <p class="msg"><%= message %></p>
      <% } else { %>
        <p class="msg">Please try again later.</p>
      <% } %>
      <p style="opacity:.6">Tip: check /healthz for DB status.</p>
    </div>
  </body>
</html>